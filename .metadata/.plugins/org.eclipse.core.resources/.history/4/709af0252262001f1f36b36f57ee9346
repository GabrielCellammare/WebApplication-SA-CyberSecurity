@charset "UTF-8";


body {
    font-family: sans-serif;
    background-color: #ececff;
}
.navbar {
    display: flex; /* Utilizza Flexbox per la disposizione degli elementi */
     /* Spazio tra il titolo e i bottoni */
    align-items: center; /* Centra verticalmente gli elementi */
    padding: 20px;
    background-color: #8ac1ff;
    border: 1px solid #001328;
}

.navbar-buttons {
    display: flex; /* Flexbox per i bottoni */
    gap: 5px; /* Distanza ridotta tra i bottoni */
    margin-left: auto; /* Allinea i bottoni alla fine della navbar */
}

.button {
    background-color: #007bff;
    color: #fff;
    padding: 10px 20px;
    text-decoration: none;
    border: 1px solid black;
    border-radius: 10px;
    font-size: 20px;
    cursor: pointer;
    text-align: center;
    margin: 0; /* Rimuove margini per ridurre ulteriormente lo spazio */
}

.welcome-message {
    text-align: center; /* Centra il messaggio di benvenuto */
    margin-top: 10px; /* Aggiunge un po' di margine sopra il messaggio */
}

.button:hover {
    background-color: #0056b3;
}



.container {
    margin: 0 auto;
    max-width: 80vw;
    padding: 5vw;
    background-color: #8ac1ff;
    opacity: 0.9;
    border-radius: 30px;
    border:1px solid #001328;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
}


.form-container {
    margin-top: 10px; /* Aggiungi uno spazio per separare il form dal resto della navbar */
}

table {
    width: auto; /* Cambia la larghezza della tabella per adattarla meglio alla navbar */
    margin-top: 10px;
}

.logout-row td {
    display: inline-block; /* Cambia da "flex" a "inline-block" per allineare i bottoni correttamente */
}



